name: Deploy to Tencent Cloud

on:
  push:
    branches: [main]
  workflow_dispatch:

concurrency:
  group: tencent-deploy
  cancel-in-progress: false

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: pnpm/action-setup@v4
        with:
          version: 9

      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: pnpm

      - run: pnpm install --frozen-lockfile

      - name: Build
        env:
          NEXT_PUBLIC_SITE_URL: ${{ secrets.NEXT_PUBLIC_SITE_URL }}
          NEXT_PUBLIC_SUPABASE_URL: ${{ secrets.NEXT_PUBLIC_SUPABASE_URL }}
          NEXT_PUBLIC_SUPABASE_ANON_KEY: ${{ secrets.NEXT_PUBLIC_SUPABASE_ANON_KEY }}
        run: pnpm build

      - name: Assemble standalone bundle
        run: |
          cp -r public .next/standalone/
          cp -r .next/static .next/standalone/.next/

      - name: Setup SSH
        run: |
          mkdir -p ~/.ssh
          echo "${{ secrets.TENCENT_SSH_KEY }}" > ~/.ssh/id_ed25519
          chmod 600 ~/.ssh/id_ed25519
          ssh-keyscan -p ${{ secrets.TENCENT_SSH_PORT || '22' }} -H ${{ secrets.TENCENT_SSH_HOST }} >> ~/.ssh/known_hosts

      - name: Snapshot previous release on server
        run: |
          ssh -i ~/.ssh/id_ed25519 -p ${{ secrets.TENCENT_SSH_PORT || '22' }} \
            ${{ secrets.TENCENT_SSH_USER }}@${{ secrets.TENCENT_SSH_HOST }} \
            "if [ -d '${{ secrets.TENCENT_DEPLOY_PATH }}' ] && [ -n \"\$(ls -A '${{ secrets.TENCENT_DEPLOY_PATH }}' 2>/dev/null)\" ]; then \
               rm -rf '${{ secrets.TENCENT_DEPLOY_PATH }}.bak' && \
               cp -a '${{ secrets.TENCENT_DEPLOY_PATH }}' '${{ secrets.TENCENT_DEPLOY_PATH }}.bak'; \
             fi"

      - name: Rsync to server
        run: |
          rsync -az --delete \
            -e "ssh -i ~/.ssh/id_ed25519 -p ${{ secrets.TENCENT_SSH_PORT || '22' }}" \
            .next/standalone/ \
            ${{ secrets.TENCENT_SSH_USER }}@${{ secrets.TENCENT_SSH_HOST }}:${{ secrets.TENCENT_DEPLOY_PATH }}/

      - name: Reload PM2
        run: |
          ssh -i ~/.ssh/id_ed25519 -p ${{ secrets.TENCENT_SSH_PORT || '22' }} \
            ${{ secrets.TENCENT_SSH_USER }}@${{ secrets.TENCENT_SSH_HOST }} \
            "cd ${{ secrets.TENCENT_DEPLOY_PATH }} && \
             (pm2 reload qdaily --update-env || PORT=${{ secrets.TENCENT_APP_PORT || '3000' }} pm2 start server.js --name qdaily --update-env) && \
             pm2 save"

      # Post-deploy acceptance test. Curls the live origin and asserts the
      # rendered HTML contains the editorial layout markers (block kinds,
      # column badge, inline markdown). If anything is missing, this step
      # exits non-zero and the deploy turns red even though PM2 reloaded ok —
      # which is exactly what we want when a silent rendering regression
      # ships (e.g. list blocks collapsing into one paragraph, **bold**
      # showing as literal asterisks). See scripts/acceptance-test.sh.
      - name: Post-deploy acceptance test
        env:
          BASE_URL: ${{ secrets.NEXT_PUBLIC_SITE_URL }}
        run: |
          # Give the freshly-reloaded PM2 a couple of seconds to warm up the
          # Next.js server before we start curling it.
          sleep 5
          bash scripts/acceptance-test.sh "${BASE_URL:-https://www.pigppy.site}"
